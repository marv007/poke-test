import { FormatIndexPipe } from './format-index.pipe';

describe('FormatIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
